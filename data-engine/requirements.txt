# data-engine 第三方依赖
# 钉版分两档：核心栈 ==精确钉（按当前环境 Python 3.13.7 实际安装版本）；
# 生态接口 >=范围钉（sqlcipher3/keyring/mcp——平台/协议兼容面，漂移风险如实接受）

# ---- AI / Agent ----
# langchain/langchain-openai/markdownify 已随图编排下线移除（20260824，全仓零引用）
langgraph==1.2.9
openai==2.45.0

# ---- Web 服务 ----
fastapi==0.138.0
uvicorn==0.49.0

# ---- 数据库 ----
chromadb==1.5.9
PyMySQL==1.1.2

# ---- 文件解析 ----
openpyxl==3.1.5
python-docx==1.2.0
PyMuPDF==1.26.7
Pillow==12.0.0
requests==2.32.5     # 云端 OCR 主路径（pipeline/ocr.py）——此前漏钉，全新机器 OCR 必炸

# ---- 本地 OCR（可选，体积大：paddlepaddle 全家桶）----
# 主路径是云端 OCR（requests 即可）；本地 OCR 仅离线/保密场景需要：
#   pip install -r requirements-ocr.txt

# ---- 配置 / 工具 ----
python-dotenv==1.2.1
PyYAML==6.0.2  # paddlex（paddleocr 依赖）硬性要求 ==6.0.2；当前环境装的 6.0.3 与 paddlex 冲突（pip check 已报），此处按可解析版本钉死

# ---- 系统工具 ----
psutil==7.1.3
pystray==0.19.5

ladybug==0.19.1  # 嵌入式图数据库（表关系可视化，20260808 替代 Neo4j）

# 数据库加密（SQLCipher）+ 密钥保管（OS 凭据管理器）
sqlcipher3>=0.5.4
keyring>=25.0

# MCP 能力面（mcp_server.py + stdio 握手测试）
mcp>=1.29,<2.0
