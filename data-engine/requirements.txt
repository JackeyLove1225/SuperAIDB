# data-engine 第三方依赖
# 版本已按当前环境（Python 3.13.7）实际安装版本钉死

# ---- AI / Agent ----
langchain==1.3.13
langchain-openai==1.3.5
langgraph==1.2.9
openai==2.45.0

# ---- Web 服务 ----
fastapi==0.138.0
uvicorn==0.49.0

# ---- 数据库 ----
chromadb==1.5.9
PyMySQL==1.1.2

# ---- 文件解析 ----
openpyxl==3.1.5
python-docx==1.2.0
PyMuPDF==1.26.7
Pillow==12.0.0
paddleocr==3.3.2  # 本地 OCR 引擎（core/parser/image_parser 可选加载；主路径为云端 OCR，见 pipeline/ocr.py）

# ---- 配置 / 工具 ----
python-dotenv==1.2.1
PyYAML==6.0.2  # paddlex（paddleocr 依赖）硬性要求 ==6.0.2；当前环境装的 6.0.3 与 paddlex 冲突（pip check 已报），此处按可解析版本钉死

# ---- 系统工具 ----
psutil==7.1.3
pystray==0.19.5

ladybug==0.19.1  # 嵌入式图数据库（表关系可视化，20260808 替代 Neo4j）

# 数据库加密（SQLCipher）+ 密钥保管（OS 凭据管理器）
sqlcipher3>=0.5.4
keyring>=25.0

# MCP 能力面（mcp_server.py + stdio 握手测试）
mcp>=1.29,<2.0
