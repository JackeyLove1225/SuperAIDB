# 全功能测试清单

## 第 1 层：编译锁（1 项）

| #   | 测试项                     | 预期   |
| --- | ----------------------- | ---- |
| 1   | 所有 .py 文件 py_compile 通过 | 0 错误 |

## 第 2 层：工具注册与路由（28 项）

| #   | 工具名                    | 测试输入                       | 预期路由                        |
| --- | ---------------------- | -------------------------- | --------------------------- |
| 1   | list_databases         | "有哪些数据库"                   | list_databases              |
| 2   | describe_schema        | "查询数据库有哪些表"                | describe_schema             |
| 3   | describe_schema        | "查询 t1 的表结构"               | describe_schema(table=t1)   |
| 4   | query                  | "查询 t1 的所有数据"              | query                       |
| 5   | query                  | "查询 t1 的 a1=100 的数据"       | query(table=t1, conditions) |
| 6   | insert_data            | "在 t1 中插入 a1=100"          | insert_data                 |
| 7   | batch_insert_data      | "向 t1 批量插入"                | batch_insert_data           |
| 8   | edit_data              | "修改 t1 的 a1 为 200"         | edit_data                   |
| 9   | delete_data            | "删除 t1 的 a1=100 的数据"       | delete_data                 |
| 10  | clear_session          | "清除会话"                     | clear_session               |
| 11  | batch_create_tables    | "新建表 t2 包含字段 b1"           | batch_create_tables         |
| 12  | batch_create_tables    | "新建表 t1（已存在）"              | 覆盖提示                        |
| 13  | create_standard_tables | "创建标准表"                    | create_standard_tables      |
| 14  | drop_table             | "删除表 t2"                   | drop_table                  |
| 15  | drop_table             | "全部删除表格"                   | drop_table(all=True)        |
| 16  | add_column             | "在 t1 中新增字段 a3，类型 TEXT"    | add_column                  |
| 17  | add_column             | "在 t1 中新增字段 a4，非空"         | add_column(not_null=True)   |
| 18  | drop_column            | "删除 t1 的 a3 字段"            | drop_column                 |
| 19  | modify_column          | "修改 t1 的 a1 类型为 TEXT"      | modify_column               |
| 20  | alter_precision        | "修改 t1 的 a1 精度为 12,2"      | alter_precision             |
| 21  | set_not_null           | "设置 t1 的 a1 为非空"           | set_not_null                |
| 22  | add_foreign_key        | "设置 t1 的 ref_col 为外键指向 t2" | add_foreign_key             |
| 23  | drop_foreign_key       | "删除 t1 的外键"                | drop_foreign_key            |
| 24  | create_index           | "设置 t1 的 a1 为索引"           | create_index(unique=True)   |
| 25  | drop_index             | "删除 t1 的索引"                | drop_index                  |
| 26  | save_template          | "保存模板"                     | save_template               |
| 27  | list_templates         | "列出模板"                     | list_templates              |
| 28  | clear_db               | "清空数据库"                    | clear_db                    |

## 第 3 层：歧义检测（5 项）

| #   | 测试输入                   | 预期         |
| --- | ---------------------- | ---------- |
| 1   | "字段 a1"（a1 在 t1、t2 都有） | 歧义提示：请指定表名 |
| 2   | "字段 b1"（b1 只在 t1）      | 自动确定 t1    |
| 3   | "字段 xyz"（不存在）          | DB 错误翻译为中文 |
| 4   | "查询表 t4 的字段 a1"（表不存在）  | 表不存在       |
| 5   | "查询表 t4"（表不存在）         | 表不存在       |

## 第 4 层：系统列保护（5 项）

| #   | 测试输入                    | 预期         |
| --- | ----------------------- | ---------- |
| 1   | "修改 t1 的 id 类型为 TEXT"   | 阻止：系统主键    |
| 2   | "删除 t1 的 id 字段"         | 阻止：系统主键    |
| 3   | "设置 t1 的 id 为非空"        | 阻止：系统主键    |
| 4   | "修改 t1 的 id 精度"         | 阻止：系统主键    |
| 5   | "在 t1 中插入 id=5, a1=100" | 阻止：不允许手动指定 |

## 第 5 层：配置一致性（7 项）

| #   | 测试                                      | 预期   |
| --- | --------------------------------------- | ---- |
| 1   | 建表后立即 _preflight_check                  | PASS |
| 2   | set_not_null 后 _preflight_check         | PASS |
| 3   | create_index 后 _preflight_check         | PASS |
| 4   | add_foreign_key 后 _preflight_check      | PASS |
| 5   | scan_utils 删 DB 表、保留 YAML → 操作被阻止       | 阻止   |
| 6   | scan_utils 改 YAML not_null、不重建表 → 操作被阻止 | 阻止   |
| 7   | scan_utils 删 DB 索引、保留 YAML → 操作被阻止      | 阻止   |

## 第 6 层：外键安全（6 项）

| #   | 测试                   | 预期        |
| --- | -------------------- | --------- |
| 1   | 设置 FK，类型不匹配          | 阻止：类型不一致  |
| 2   | 设置 FK，目标表不存在         | 阻止        |
| 3   | 删除被其他表 FK 引用的列       | 阻止：先解除 FK |
| 4   | 删除被其他表 FK 引用的表       | 阻止        |
| 5   | 修改 FK 列的类型           | 阻止        |
| 6   | FK 强制删除 + force=true | 通过        |

## 第 7 层：数据操作（8 项）

| #   | 测试                   | 预期          |
| --- | -------------------- | ----------- |
| 1   | 插入正常数据               | 成功          |
| 2   | 插入重复值到 UNIQUE 列      | 违反唯一性约束     |
| 3   | 插入 NULL 到 NOT NULL 列 | 违反非空约束      |
| 4   | 查询空表                 | "查询内容为空"    |
| 5   | 查询有数据的表              | Markdown 表格 |
| 6   | 条件查询（WHERE）          | 正确筛选        |
| 7   | 批量插入                 | 成功          |
| 8   | 修改数据                 | 成功          |

## 第 8 层：端到端（每工具多场景覆盖）

### 8.1 查数据库（3 项）

| #   | 输入         | 预期               |
| --- | ---------- | ---------------- |
| 1   | "安装了哪些数据库" | sqlite / 类型 / 路径 |
| 2   | "有哪些数据库"   | 同上               |
| 3   | "查询数据库信息"  | 同上               |

### 8.2 查表结构 describe_schema（5 项）

| #   | 输入                   | 预期                |
| --- | -------------------- | ----------------- |
| 1   | "查询数据库有哪些表"          | Markdown 表格       |
| 2   | "查询 t1 的表结构"         | 只显示 t1 的字段        |
| 3   | "查询 t1 的 a1 字段类型"    | a1 的数据类型是 INTEGER |
| 4   | "查询 t1 的 xyz_col 字段" | 字段不存在             |
| 5   | "查询表 xyz999"（表不存在）   | 表不存在              |

### 8.3 查数据 query（6 项）

| #   | 输入                                | 预期             |
| --- | --------------------------------- | -------------- |
| 1   | "查询 t1 的所有数据"                     | 数据行 + Markdown |
| 2   | "查询 t1 的 a1=100 的数据"              | 筛选后的行          |
| 3   | "查询 t1 的 a1 大于 50 且 a2 等于 hi 的数据" | 多条件 AND        |
| 4   | "查询 t1 中 a1 最大的前 3 条"             | 排序+限制          |
| 5   | "查询 t1"（空表）                       | 暂无数据           |
| 6   | "查询 xyz999"（表不存在）                 | 表不存在           |

### 8.4 插入数据 insert_data（6 项）

| #   | 输入                           | 预期              |
| --- | ---------------------------- | --------------- |
| 1   | "在 t1 中插入 a1=5, a2=hello"    | 已插入             |
| 2   | "在 t1 中插入 a1=100"（重复 UNIQUE） | 违反唯一性           |
| 3   | "在 t1 中插入 a2=hello"（跳过非空列）   | 违反非空 / 成功（看列定义） |
| 4   | "在 t1 中插入 id=9,a1=10"        | 不允许手动指定 id      |
| 5   | "向表 xyz 插入 a1=1"（表不存在）       | 表不存在            |
| 6   | "插入 a1=1"（未指定表名）             | 请指定表名           |

### 8.5 批量插入 batch_insert_data（2 项）

| #   | 输入                               | 预期      |
| --- | -------------------------------- | ------- |
| 1   | "向 t1 批量插入：a1=1,a2=a; a1=2,a2=b" | 已插入 2 条 |
| 2   | "向 xyz 批量插入 a1=1"                | 表不存在    |

### 8.6 修改数据 edit_data（4 项）

| #   | 输入                               | 预期                 |
| --- | -------------------------------- | ------------------ |
| 1   | "修改 t1 中 a1=5 的数据，将 a2 改为 world" | 已修改                |
| 2   | "修改 t1 的数据"（未指定条件）               | WHERE 不安全 / 全表修改风险 |
| 3   | "修改 xyz 表的数据"（表不存在）              | 表不存在               |
| 4   | "修改 t1 的 id 为 10"                | 不允许修改系统列           |

### 8.7 删除数据 delete_data（4 项）

| #   | 输入                    | 预期           |
| --- | --------------------- | ------------ |
| 1   | "删除 t1 中 a1=5 的数据"    | 已删除 N 条      |
| 2   | "清空 t1 的所有数据"（不含"删表"） | 已从 t1 删除 N 条 |
| 3   | "删除 xyz 的数据"（表不存在）    | 表不存在         |
| 4   | "删除 t1 中 被外键引用的行"     | 删除阻止         |

### 8.8 会话 clear_session（1 项）

| #   | 输入       | 预期  |
| --- | -------- | --- |
| 1   | "清除会话记录" | 已清除 |

### 8.9 建表 batch_create_tables（5 项）

| #   | 输入                             | 预期                   |
| --- | ------------------------------ | -------------------- |
| 1   | "新建表 t3 包含字段 c1(int),c2(text)" | 已创建 + id 自动字段        |
| 2   | "新建表 t3 包含字段 d1"（同名表）          | 覆盖提示 → y/yes 覆盖 → 成功 |
| 3   | "新建表 t3 含 d1" → "no"           | 已取消                  |
| 4   | "按照以下建表：8张表含FK关联"              | 一次性建多表+FK            |
| 5   | "新建空表"（无字段）                    | 至少 id 字段             |

### 8.10 标准表 create_standard_tables（1 项）

| #   | 输入      | 预期         |
| --- | ------- | ---------- |
| 1   | "创建标准表" | 已创建 / 无需操作 |

### 8.11 删表 drop_table（4 项）

| #   | 输入                | 预期        |
| --- | ----------------- | --------- |
| 1   | "删除表 t3"          | 已删除       |
| 2   | "全部删除表格"          | 已删除 N 个表  |
| 3   | "删除表 xyz999"（不存在） | 表不存在      |
| 4   | "删除被外键引用的表"       | 阻止：先解除 FK |

### 8.12 加字段 add_column（5 项）

| #   | 输入                       | 预期                  |
| --- | ------------------------ | ------------------- |
| 1   | "在 t1 中新增 a4 字段，类型 TEXT" | 已加入                 |
| 2   | "在 t1 中新增 a5 字段，INT，非空"  | 已有数据→"请先补全" / 空表→成功 |
| 3   | "在 t1 中新增字段 a1"（重复）      | 已存在                 |
| 4   | "新增字段 name"（未指定表）        | 请指定表名               |
| 5   | "在 xyz 中加字段 a1"（表不存在）    | 表不存在                |

### 8.13 删字段 drop_column（4 项）

| #   | 输入                    | 预期                   |
| --- | --------------------- | -------------------- |
| 1   | "删除 t1 的 a4 字段"（普通字段） | 已删除                  |
| 2   | "删除 t1 的 id 字段"       | 阻止：系统主键              |
| 3   | "删除 t1 的外键字段"         | 提示确认 → force=true 删除 |
| 4   | "删除 t1 的被 FK 引用的字段"   | 阻止：先解除外键             |

### 8.14 改字段类型 modify_column（4 项）

| #   | 输入                         | 预期             |
| --- | -------------------------- | -------------- |
| 1   | "修改 t1 的 a2 为 INTEGER"     | force 提示 / 已修改 |
| 2   | "修改 t1 的 id 为 TEXT"        | 阻止：系统主键        |
| 3   | "修改 xyz 的 a1 为 TEXT"（表不存在） | 表不存在           |
| 4   | "修改 t1 的 a1 为 VARCHAR"（同族） | 无风险，直接改        |

### 8.15 改精度 alter_precision（2 项）

| #   | 输入                    | 预期      |
| --- | --------------------- | ------- |
| 1   | "修改 t1 的 a1 精度为 10,2" | 已修改     |
| 2   | "修改 t1 的 id 精度"       | 阻止：系统主键 |

### 8.16 设非空 set_not_null（4 项）

| #   | 输入                       | 预期          |
| --- | ------------------------ | ----------- |
| 1   | "设置 t1 的 a1 为非空"（a1 无空值） | 已设为非空       |
| 2   | "设置 t1 的 a2 为非空"（a2 有空值） | 存在空值，无法设为非空 |
| 3   | "设置 t1 的 id 为非空"         | 已是/阻止       |
| 4   | "设置 xyz 的 a1 为非空"（表不存在）  | 表不存在        |

### 8.17 加外键 add_foreign_key（4 项）

| #   | 输入                           | 预期         |
| --- | ---------------------------- | ---------- |
| 1   | "在 t1 新建 FK 字段 ref_t2 指向 t2" | 已设置外键      |
| 2   | "在 t1 设置外键指向 xyz"（目标表不存在）    | 表不存在       |
| 3   | "在 t1 设 FK（类型不匹配）"           | 类型不一致，无法设置 |
| 4   | FK 字段匹配列名 → 指向该列；不匹配 → 指向 id |            |

### 8.18 删外键 drop_foreign_key（2 项）

| #   | 输入                 | 预期     |
| --- | ------------------ | ------ |
| 1   | "删除 t1 的外键 ref_t2" | 已删除    |
| 2   | "删除 t1 的外键"（未指定哪个） | 请指定字段名 |

### 8.19 加索引 create_index（4 项）

| #   | 输入                       | 预期         |
| --- | ------------------------ | ---------- |
| 1   | "设置 t1 的 a1 为索引"（a1 无重复） | 已创建唯一索引    |
| 2   | "设置 t1 的 a2 为索引"（a2 有重复） | 存在重复值，无法创建 |
| 3   | "设置 t1 的 a1 为普通索引"       | 已创建普通索引    |
| 4   | "设置 t1 的 id 为索引"         | PK 自带索引    |

### 8.20 删索引 drop_index（1 项）

| #   | 输入                    | 预期  |
| --- | --------------------- | --- |
| 1   | "删除 t1 的索引 idx_t1_a1" | 已删除 |

### 8.21 模板操作（3 项）

| #   | 输入                   | 预期   |
| --- | -------------------- | ---- |
| 1   | "保存当前表结构为模板 my_tmpl" | 已保存  |
| 2   | "列出所有模板"             | 模板列表 |
| 3   | "导入模板 my_tmpl"       | 已导入  |

### 8.22 清库 clear_db（3 项）

| #   | 输入                  | 预期           |
| --- | ------------------- | ------------ |
| 1   | "清空数据库"（删全部表）       | 已删除 N 个表     |
| 2   | "清空 t1 的所有数据"（只清数据） | 已从 t1 删除 N 条 |
| 3   | "清空"（未指定）           | 路由正确         |

## 第 9 层：跨切面（7 项）

| #   | 输入                  | 预期              |
| --- | ------------------- | --------------- |
| 1   | "字段 a1"（a1 在多表）     | 歧义：请指定表名        |
| 2   | "字段 b1"（b1 唯一）      | 自动确定表，返回类型/数据   |
| 3   | "查询表 xyz999"        | 不存在             |
| 4   | "修改 t1 的 id 为 TEXT" | 不允许             |
| 5   | "删除 t1 的 id"        | 不允许             |
| 6   | "在 t1 中插入 id=9"     | 不允许             |
| 7   | 任意错误消息含             | [trace:req-xxx] |

---

**总计：~138 项测试**（28 工具 × 多场景 + 跨切面 7 项）
