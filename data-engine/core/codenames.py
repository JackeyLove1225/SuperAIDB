"""开发过程代号编年史正则——全仓唯一真源（single source of truth）

使用方：tests/test_39_doc_coherence.py（层 39 代号机检）、
根仓 sync_showcase.py（发布闸：真源文件/前端面/commit message 三处）。
三方引用同一正则与同一扫描扩展名集合，防"模式清单多处复制后漂移"病型。
"""
import re

# 迭代代号编年史：评审/收敛/运维 N轮、裸 N轮（十三轮/13轮）、N轮续、
# 方案 A-F、P1-n/T1.n/F-n/D-n、事故归因词——技术陈述保留、归因标签清零。
# 裸 N轮 的排除面：本轮/一轮/每轮/上下前后那这周轮+轮（正常时间语义——
# 后向否定）+ 轮后负向前瞻（三轮询/两轮转发 这类正常词不误伤）。
CODENAME_RE = re.compile(
    r"评审[一二三四五六七八九十\d]+轮|收敛[一二三四五六七八九十\d]+轮|"
    r"(?<![本一每上下前后那这周轮])(?:[二两三四五六七八九十]{1,3}轮|十一轮|二十[一二三四五六七八九]?轮)(?!询|转|流)|\d+轮(?!询|转|流)|"
    r"[一二三四五六七八九十]+轮续|方案[A-F]|P[12]-\d+|T1\.\d+|"
    r"（F[1-7]|F[1-7][：:]|（[DP]\d+）|运维[一二三四五六七八九十]+轮|"
    r"上轮事故|实测事故|事故根因")

# 代号机检的扫描扩展名集合（层 39 与发布闸共用）
CODENAME_SCAN_EXTS = (".md", ".py", ".ts", ".tsx", ".yml", ".yaml",
                      ".json", ".js", ".bat", ".toml", ".txt", ".sql", ".ps1")
