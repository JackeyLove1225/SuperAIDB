"""数据库加密边界（core/crypto）——独立解耦模块，可整体拔除

数据主权边界 = 加密边界：
- 边界内（本产品的库文件）：静态密文（SQLCipher AES-256），密钥由 OS 凭据
  管理器保管（keyring），任何不持密钥的读取（直接打开/拷贝/误提交）都是乱码
- 边界外（未接入的文件/服务型库）：OS 层的事，本产品不管也不背锅
- 接入即加密：DB_ENCRYPT=true 时，明文库在打开瞬间自动迁移为密文（留明文备份）

解耦边界：上层（驱动/契约/权限/工具/AI）对加密零感知——所有连接只经
`open_db()` 工厂产生，原生 sqlite3 与 SQLCipher 对调用方 API 一致。

模块结构：
- key_manager：主密钥的生成/存取（OS 凭据管理器，用户零操作）
- connection：open_db 连接工厂（全仓唯一连接入口）
- migrate：明文检测 + 自动迁移（含 WAL  checkpoint 与备份）
"""
